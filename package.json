{
  "name": "castro-ai",
  "version": "1.0.0",
  "private": true,
  "description": "Castro AI — centro de comando con agentes de IA para Team Fernando Castro (RE/MAX)",
  "scripts": {
    "dev": "concurrently -n client,server -c blue,red \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "install:all": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
